{
  "builds": [
    {
      "src": "jobnest/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "jobnest/wsgi.py"
    }
  ],
  "buildCommand": "pip install -r requirements.txt && pip install psycopg2-binary==2.9.6 && python manage.py collectstatic --noinput"
}
